# Ralph-BMAD Progress — TrailBlazeAI

Last updated: 2026-02-20
Current story: 4-2-quiz-review-panel-with-approve-edit-workflow
Current phase: ORIENT

## Summary
- Stories complete: 14/18
- Stories blocked: 0
- Current sprint: 4
- Total iterations: 1
- Architect escalations: 0

## Current Story
- Story: 4-2-quiz-review-panel-with-approve-edit-workflow
- Phase: ORIENT
- Task: 0/0 complete
- QA cycles: 0
- Review cycles: 0
- Blockers: none

## Codebase Patterns (Accumulated Learnings)

<!-- Append patterns discovered during implementation here -->
- PipelineClient structural type avoids generated-type lag (from Stories 2.x)
- `as unknown as PipelineClient` is the established cast pattern — never use `as any`
- pg-boss `send()` for job chaining belongs in queue-handlers.ts, NOT in stage functions
- Supabase `insert()` takes an array, not a single object: `db.from('table').insert([rowObject])`
- YAML prompts loaded at module level (not per-call) for performance — use module-level cache with `_resetPromptCache()` for testing
- `BossWithWork` structural type is already defined in queue-handlers.ts — reuse it
- Queue config examples: `{ retryLimit: 2, retryBackoff: true, expireInHours: 0.5 }` for AI stages
- Queues are created at API startup in `apps/api/src/plugins/pg-boss.ts` (registered via `app.ts`)
- `node-html-parser` is already installed for HTML parsing
- Handler errors: `throw err` makes pg-boss retry; `return` prevents retries
- ChonkieJS (`@chonkiejs/core`) does NOT support chunk overlap natively — implement manual overlap in calling code
- `String.prototype.replace` callback provides `offset` as second arg (after match) — use it instead of `indexOf` for duplicate-safe position lookups
- Always use Zod `safeParse` at system boundaries for JSONB data written by LLM calls — never trust `as` cast
- `conceptExtractionSchema` from `knowledge-agent.ts` is the canonical Zod schema for sf_concepts validation
- Guard against empty chunk arrays before calling `supabase.insert()` — some clients reject empty inserts
- AI SDK versioning: `ai@6` uses V3 provider protocol. Use `@ai-sdk/openai@^2` (V2 → compatible) NOT `^1` (V1 → incompatible). With `^2`, `openai.embedding()` returns `EmbeddingModelV2<string>` which is in `ai@6`'s `EmbeddingModel` union — no cast required.
- Structural type for `.eq().is()` chaining: `eq()` must return `{ is(col, val): Promise<...> }` (not just a Promise) to support `embedding IS NULL` filter
- FTS trigger: Use `DROP TRIGGER IF EXISTS` before `CREATE TRIGGER` to make migrations idempotent (re-runnable on dev resets)
- startTime should be captured BEFORE the first async operation (not after guard clauses) so all log paths record real elapsed time
- `vi.useFakeTimers()` deadlocks with React 18 + Vitest `act()` — React 18 scheduler uses `setTimeout` internally. Fix: use real timers with `waitFor({ timeout: 600 })` to naturally wait through debounce delays (e.g. 300ms debounce → 600ms timeout)
- `vi.hoisted(() => vi.fn())` is required to capture mock functions that `vi.mock()` factory closures reference — must be called before the vi.mock() call due to ESM hoisting order
- Next.js App Router: each route segment needs its own `layout.tsx` to include shared components (e.g. CommandMenu) — sibling layouts do NOT propagate across route segments
- TypeScript interface drift risk: define interfaces once in the primary component file, re-export via `import type` in consuming files — never duplicate interface declarations
- `String.replaceAll()` vs `replace()` — use `replaceAll` for underscore-to-space or any multi-occurrence substitution
- shadcn command component: install via `pnpm dlx shadcn@latest add command --cwd apps/web`; installs `cmdk` peer dependency automatically
- ContentTypeIcon switch: always include explicit `'definition'` case — never rely on default fallback for known enum values
- YAML mocks in Vitest: avoid YAML double-quoted inline strings containing `{placeholder}` — YAML parser treats `}` at end-of-line as block indicator → "Missing closing quote" error. Use YAML single-quoted strings (`'...'`) in test `readFile` mocks; block scalars (`|`) are safe in actual YAML files.

## Completed Stories

### Sprint 1: Foundation (Epic 1) — COMPLETE
- **1-1-api-foundation-and-docker-deployment**: done (commit 400e938)
- **1-2-frontend-shell-and-authentication**: done (commit 400e938)
- **1-3-trailmix-import-and-module-discovery**: done (commit 400e938)
- **1-4-real-time-module-status-dashboard**: done (commit 400e938)
- Note: All 4 stories batch-committed in single session — quality review pending

### Sprint 2: Content Extraction (Epic 2) — COMPLETE
- **2-1-playwright-mcp-integration-and-browser-session-management**: done (commit 941a557)
- **2-2-unit-content-extraction-pipeline**: done (commit 941a557)
- **2-3-pipeline-concurrency-and-retry-management**: done (commit 941a557)
- **2-4-session-expiry-detection-and-recovery**: done (commit 941a557)
- Note: All 4 stories batch-committed in single session — quality review pending

### Sprint 3: Knowledge Processing (Epic 3) — COMPLETE
- **3-1-content-chunking-with-salesforce-specific-rules**: done
  - Completed: 2026-02-20
  - Tasks: 8/8
  - Tests added: ~40 (knowledge-agent: 13, identify-concepts: 7, chunk-content: 21)
  - Files created: 8 new, 4 modified
  - Review findings: 4 HIGH fixed, 4 MEDIUM deferred, 2 LOW deferred
  - QA cycles: 1
  - Review cycles: 1 (CHANGES_REQUESTED → fixed → pass)
  - Architect escalations: 0

- **3-2-embedding-generation-and-vector-storage**: done
  - Completed: 2026-02-20
  - Tasks: 5/5
  - Tests added: 10 (generate-embeddings: 10) + 5 queue handler tests
  - Files created: 3 new, 3 modified
  - Review findings resolved: 2 HIGH fixed (V1/V2 provider upgrade to @ai-sdk/openai@^2, embedding payload assertion); 2 MEDIUM fixed (migration idempotency, startTime before query). Deferred: N+1 UPDATE loop (performance, not correctness), `as any` in test mocks (LOW), unit_id index (LOW/future migration).
  - QA cycles: 2 (first cycle found IS NULL filter missing + model assertion missing)
  - Review cycles: 1 (CHANGES_REQUESTED → fixed → pass implied)
  - Architect escalations: 0

- **3-3-concept-relationship-mapping**: done
  - Completed: 2026-02-20
  - Commit: 86bdc04
  - Tasks: 5/5
  - Tests added: 12 (build-relationships: 10, queue handler: 2 module-status-transition)
  - Files created: 3 new (migration 011, build-relationships.ts, build-relationships.test.ts)
  - Files modified: 9
  - Review findings resolved: 2 HIGH fixed (N+1 chunk queries eliminated + error check on all-units fetch), 1 MEDIUM fixed (prompt existence assertion). Deferred: `as any` in queue-handler tests (LOW, pre-existing), inline structural type duplication (LOW), sf_api_names excluded from concept list (MEDIUM, documented).
  - QA cycles: 1 (missing module-status-transition tests discovered)
  - Review cycles: 1 (CHANGES_REQUESTED → fixes applied → pass)
  - Architect escalations: 0

- **3-4-hybrid-search-and-knowledge-api**: done
  - Completed: 2026-02-20
  - Tasks: 4/4
  - Tests added: 10 (knowledge route: 10)
  - Files created: 3 new (migration 012, knowledge.ts, knowledge.test.ts)
  - Files modified: 2 (app.ts wiring, story file)
  - Review findings resolved: 3 HIGH fixed — (1) Zod sf_topics `.nullish().transform(v => v ?? [])` coerces null to [] correctly; (2) batch enrichment errors logged with `app.log.warn()` instead of silent discard; (3) AC3 test strengthened to assert exact module_name/unit_title/relevance_score values. Deferred: MEDIUM1 — `websearch_to_tsquery` tsquery CTE optimization (performance); MEDIUM2 — Supabase client per-request (consistent with modules.ts pattern); LOW1 — redundant ORDER BY rank in CTEs (cosmetic); LOW2 — `results[0]` index guarded by `expect(result).toBeDefined()`.
  - QA cycles: 1 (READY_FOR_REVIEW)
  - Review cycles: 1 (CHANGES_REQUESTED → 3 HIGH fixed → pass)
  - Architect escalations: 0

- **3-5-knowledge-base-ui-with-global-search**: done
  - Completed: 2026-02-20
  - Tasks: 6/6
  - Tests added: 15 (knowledge-search-panel: 9, command-menu: 6)
  - Files created: 12 new (api/knowledge/search/route.ts, knowledge-search-panel.tsx+test, chunk-card.tsx, chunk-detail.tsx, knowledge/index.ts, command-menu.tsx+test, knowledge/layout.tsx, settings/layout.tsx, ui/command.tsx, ui/dialog.tsx)
  - Files modified: 5 (knowledge/page.tsx, knowledge/loading.tsx, dashboard/layout.tsx, layout/index.ts, story file)
  - Review findings resolved: 4 HIGH fixed (real timers for debounce, enrichedSelectedChunk for related chunks, vi.hoisted() for mockPush, single-source KnowledgeSearchResult), 2 MEDIUM fixed (replaceAll, definition case). QA BLOCKER fixed: layout.tsx files for /knowledge and /settings routes. Deferred: HIGH-1 real-wall-clock waits (React 18+Vitest incompatibility, documented).
  - QA cycles: 2 (NEEDS_WORK → fixes → READY_FOR_REVIEW)
  - Review cycles: 2 (CHANGES_REQUESTED → fixes → pass)
  - Architect escalations: 0

- **4-1-quiz-agent-with-chain-of-thought-reasoning**: done
  - Completed: 2026-02-20
  - Tasks: 5/5
  - Tests added: 14 (quiz-agent: 12, queue-handler: 2)
  - Files created: 3 new (quiz-agent.yaml, quiz-agent.ts, quiz-agent.test.ts)
  - Files modified: 2 (queue-handlers.ts, queue-handlers.test.ts)
  - Review findings: pending
  - QA cycles: 0
  - Review cycles: 0
  - Architect escalations: 0

## Blocked Stories
(none)

## Quality Review Log
<!-- Track adversarial reviews of completed stories here -->
- Epic 1+2: Pending quality review (stories were implemented in a single long session without per-story commits)
- Story 3-1: Reviewed by ralph-reviewer. 4 HIGH issues fixed: (1) manual chunk overlap for ChonkieJS, (2) extractCodeBlocks offset parameter, (3) Zod safeParse for sf_concepts JSONB, (4) stronger test assertion for module status update. Deferred: CHUNK_OVERLAP via library API (library limitation), `as any` in test mocks (LOW), HANDS_ON_MAX_TOKENS unused (LOW), tool_type semantic mismatch (MEDIUM), module status race condition (MEDIUM).
- Story 3-2: Reviewed by ralph-reviewer. 2 HIGH issues fixed: (1) upgraded @ai-sdk/openai from ^1 to ^2 to resolve V1/V2 provider protocol mismatch (enables native type compatibility, no cast needed), (2) added embedding payload assertion to update test. 2 MEDIUM fixed: (3) DROP TRIGGER IF EXISTS for idempotent migration, (4) startTime captured before Supabase select. Deferred: N+1 UPDATE loop (perf, not correctness), `as any` in test mocks, unit_id index.
- Story 3-3: Reviewed by ralph-reviewer. 2 HIGH fixed: (1) N+1 chunk lookup loop eliminated — single `sf_knowledge_chunks` query hoisted, in-memory `sf_topics` matching per concept, source and target chunk can differ; (2) error check added on all-units module fetch (throws PipelineError instead of silently degrading). 1 MEDIUM fixed: (3) build_relationships prompt existence asserted (throws PipelineError if YAML key missing). Deferred: `as any` in queue-handler tests (LOW, pre-existing pattern), sf_api_names excluded from concept list (MEDIUM, scope-appropriate), AC2 cross-MODULE scope noted as within-module (Dev Notes document this as intentional).
- Story 3-4: Reviewed by ralph-reviewer. 3 HIGH fixed: (1) Zod sf_topics schema uses `.nullish().transform(v => v ?? [])` — `.default([])` only fires on undefined not null; (2) batch enrichment errors (units, modules, relationships) now logged with `app.log.warn()` instead of silently discarded; (3) AC3 test strengthened to assert exact `module_name`, `unit_title`, and `relevance_score` values from mock data. Deferred: MEDIUM — `websearch_to_tsquery` called twice in CTEs (tsquery CTE optimization); MEDIUM — Supabase client instantiated per request (consistent with modules.ts template); LOW — redundant ORDER BY rank before LIMIT in CTEs; LOW — `results[0]` array access in test (guarded by `expect(result).toBeDefined()`).
- Story 3-5: Reviewed by ralph-reviewer. 4 HIGH fixed: (1) Replaced `vi.useFakeTimers()` with real timers + `waitFor({ timeout: 600 })` — React 18 scheduler uses setTimeout internally causing deadlock with fake timers; (2) Added `enrichedSelectedChunk` filtering `related_chunk_ids` to only IDs present in current `results` — eliminates silent no-op on related chunk click; (3) Used `vi.hoisted()` to capture `mockPush` for navigation assertion in command-menu tests; (4) Eliminated `KnowledgeSearchResult` interface triplication — consolidated to single source in `knowledge-search-panel.tsx`, consumed via `import type` in chunk-card/chunk-detail. 2 MEDIUM fixed: (5) `replaceAll('_', ' ')` in chunk-detail.tsx; (6) explicit `'definition'` case added to ContentTypeIcon switch. QA BLOCKER fixed: CommandMenu availability extended to /knowledge and /settings routes via dedicated layout.tsx files. Deferred: HIGH-1 debounce test real-wall-clock waits (React 18 incompatibility, documented as accepted pattern).
